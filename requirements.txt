# Sentinel-2 Satellite Data Downloader with Google Earth Engine

# Core dependencies for Google Earth Engine
earthengine-api>=0.1.384
geemap>=0.32.0

# Geospatial data processing
geopandas>=0.12.0
rasterio>=1.3.0
numpy>=1.21.0
requests>=2.28.0
shapely>=2.0.0
urllib3<2.0          # Compatible with earthengine-api
fiona>=1.8.0
pyproj>=3.4.0
sentinelhub>=3.9.0   # For Sentinel Hub API integration

# Configuration and utilities
python-dotenv>=0.19.0  # For .env file support
tqdm>=4.64.0          # For progress bars

# Visualization dependencies
matplotlib>=3.5.0     # For plotting and visualization
pandas>=1.3.0         # For data manipulation

# Additional dependencies for pixel extraction and file processing
Pillow>=9.0.0          # Image processing support
GDAL>=3.4.0            # Geospatial Data Abstraction Library (for rasterio)
# Note: On Windows, consider installing GDAL from conda-forge or OSGeo4W