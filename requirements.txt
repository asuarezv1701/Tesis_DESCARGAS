# Sentinel-2 Satellite Data Downloader with Google Earth Engine
# INSTALACIÓN: pip install -r requirements.txt
# OPCIÓN ALTERNATIVA: conda env create -f environment.yml (ver NOTAS_CONDA.txt en raíz)

# Core dependencies for Google Earth Engine
earthengine-api>=0.1.384
geemap>=0.32.0

# Geospatial data processing
geopandas>=0.12.0
rasterio>=1.3.0
numpy>=1.21.0
requests>=2.28.0
shapely>=2.0.0
urllib3<2.0          # Compatible with earthengine-api
fiona>=1.8.0
pyproj>=3.4.0
sentinelhub>=3.9.0   # For Sentinel Hub API integration
rtree>=1.0.0          # Spatial index for geopandas on Windows
pyogrio>=0.7.2        # Fast I/O for GeoPackage/GeoParquet

# Configuration and utilities
python-dotenv>=0.19.0  # For .env file support
tqdm>=4.64.0          # For progress bars

# Visualization dependencies
matplotlib>=3.5.0     # For plotting and visualization
pandas>=1.3.0         # For data manipulation

# Additional dependencies for pixel extraction and file processing
Pillow>=9.0.0          # Image processing support
# Nota: evitamos instalar el paquete "GDAL" vía pip en Windows,
# ya que las ruedas de rasterio/fiona incluyen las bibliotecas nativas de GDAL.
# Si en el futuro necesitas importaciones directas de osgeo/gdal, instala una wheel precompilada de GDAL.